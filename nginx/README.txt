This contains the copies nginx configurations for each server. Whenever changes are made to them, they should be copied into their respective folders dev, prod, or connect.

basic/default.conf is the one to use to set up a simple port 80 site. 
